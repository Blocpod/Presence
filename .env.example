# Everything works locally without API keys or a paid provider.
PRESENCE_PROVIDER=demo
# Required only for `npm start` (production-build localhost preview).
PRESENCE_ENABLE_LOCAL_DEMO=true
# Optional SQLite path; never commit the database or cookies.
# PRESENCE_DB_PATH=.data/presence.sqlite
